# Core Change Rules

- Only root may edit core files.
- Non-root nodes emit votes.
- Root aggregates votes.
- Core changes require repeated signal or safety-critical evidence.
- Core changes must bump core version in affected files or notes.
- Core changes must run self-test.
