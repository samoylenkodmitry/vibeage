# VPS Audit 2026-05-07

Host: `159.69.33.249`

## Summary

Production is wired to the VPS, not Vercel. The VPS serves the static frontend with Nginx from `/opt/vibeage-frontend/out` and proxies game traffic to the Dockerized game server on `localhost:3001`.

GitHub-hosted SSH deployment was disabled and its temporary deploy key was revoked on 2026-05-07. Use the local deploy script from a trusted workstation instead.

Do not run the old generated setup scripts on this host without first preserving the existing Nginx and mail configuration. The live Nginx config is hand-edited and the Stalwart mail vhost is separate from the game deployment path.

## Verified Live Services

- `nginx.service`: active, public on `80` and `443`.
- `docker.service`: active.
- `vibeage-game-1`: Docker container, bound to `127.0.0.1:3001`.
- `vibeage-db-1`: Docker container, bound to `127.0.0.1:5432`.
- `stalwart`: Docker container, public mail ports on `25`, `143`, `465`, `587`, and `993`; raw admin/web port `8080` is bound to `127.0.0.1`.
- `wg-quick@wg0.service`: stopped and disabled; `/etc/wireguard/wg0.conf` was removed.
- Old Lineage stream listeners on `2106` and `7777`: disabled by moving the Nginx stream include out of `/etc/nginx/stream-enabled`.
- `netfilter-persistent.service`: active and enabled; host firewall INPUT policy is default-drop for IPv4 and IPv6.
- `fail2ban.service`: active and enabled with `sshd`, `nginx-http-auth`, and `nginx-limit-req` jails.

Current public listeners after cleanup are SSH, Nginx, and intended Stalwart mail ports only. The host firewall allow-list permits TCP `22`, `25`, `80`, `143`, `443`, `465`, `587`, and `993`, plus required loopback, established traffic, ICMP, IPv6 ICMP, and DHCP client replies. The game server, database, DNS stub listeners, and Stalwart `8080` are localhost-only.

## Verified Nginx Wiring

Readable vhost: `/etc/nginx/sites-available/vibeage.eu`

- `location /` serves static files from `/opt/vibeage-frontend/out`.
- `location /socket.io/` proxies to `http://localhost:3001`.
- `location /api/` proxies to `http://localhost:3001`.
- TLS is managed by Certbot for `vibeage.eu`.
- The old public `/l2.ini` Lineage file was moved into `/root/vibeage-security-backups`; `https://vibeage.eu/l2.ini` now returns `404`.

Mail vhost:

- `/etc/nginx/sites-available/mail.dmitrysamoylenko.in` exists and is enabled.
- It proxies HTTPS traffic to Stalwart on `127.0.0.1:8080`.
- Because mail config exists and Stalwart is live, scripts must not overwrite or reload Nginx blindly.
- Stalwart mail protocol TLS uses the existing Let's Encrypt certificate for `mail.dmitrysamoylenko.in`; do not remove its read-only certificate mounts from `/opt/stalwart/docker-compose.yml`.

## Git And Deploy State On VPS

Active deploy checkout: `/home/s/vibeage-deploy/repo`

- Current branch: `main`.
- Current deployed commit during this audit update: `f9fe805955e1`.
- Deployment marker: `~/.vibeage-deploy/last-deploy.json`.

Legacy checkout: `/opt/vibeage`

- Archived to `/root/vibeage-security-backups/legacy-opt-vibeage-20260507T144752Z.tar.gz`.
- Removed from `/opt`.
- Do not use old generated update scripts as the production update path.

Legacy frontend path: `/opt/vibeage-frontend`

- Static public directory `/opt/vibeage-frontend/out` is still the Nginx document root.
- The current local deploy script publishes built frontend files into this existing directory without rewriting Nginx.
- Old checkout metadata was archived to `/root/vibeage-security-backups/legacy-opt-vibeage-frontend-20260507T144752Z.tar.gz`.
- `/opt/vibeage-frontend` now intentionally contains only `out`.

## Cron And Systemd Inspection

- User `s` has a Postgres backup crontab entry that runs daily from `/home/s/vibeage-deploy/repo`.
- No app-specific user systemd service or timer was found.
- Root/system timers observed were normal system maintenance plus Certbot renewal.
- No `vibeage`, Lineage, or WireGuard cron job was found.
- The active deploy path is `/home/s/vibeage-deploy/repo`.

## Changes Made During Cleanup

- Removed GitHub repository secrets for VPS deployment and removed the temporary GitHub deploy public key from the VPS user's `authorized_keys`.
- Deleted the GitHub Actions deploy workflow; only CI and Dependabot remain active.
- Rebound the game container to `127.0.0.1:3001`.
- Kept Postgres on `127.0.0.1:5432`.
- Moved the Lineage Nginx stream config for `2106` and `7777` out of the enabled stream directory, then tested and reloaded Nginx.
- Changed Stalwart Docker Compose port `8080` to `127.0.0.1:8080:8080`, then recreated the Stalwart container.
- Stopped and disabled `wg-quick@wg0`, then removed the old WireGuard config file.
- Applied and persisted a host firewall allow-list with default-drop INPUT policy for IPv4 and IPv6.
- Removed stale firewall accepts and NAT leftovers for WireGuard and Lineage ports.
- Moved the stale Lineage stream symlink and public `/l2.ini` file into `/root/vibeage-security-backups`.
- Installed daily Postgres backups under `/home/s/.vibeage-backups/postgres` and verified restore into an isolated temporary container.
- Installed a workstation-side daily backup pull into `/media/huge/vibeage-backups/postgres`, keeping only the newest two local copies.
- Archived and removed `/opt/vibeage`.
- Archived old `/opt/vibeage-frontend` metadata and preserved only `/opt/vibeage-frontend/out`.
- Verified the local deploy path after legacy `/opt` cleanup.
- Wired Stalwart to the existing Let's Encrypt certificate for `mail.dmitrysamoylenko.in` after mail clients saw the generated self-signed `rcgen` certificate on `993`/`587`.
- Added `/etc/letsencrypt/renewal-hooks/deploy/restart-stalwart.sh` so Stalwart restarts after future certbot renewals and reloads the renewed certificate files.
- Verified `993`, `587`, `465`, and `143` with OpenSSL; all present a trusted certificate for `mail.dmitrysamoylenko.in` after the fix.
- Verified `https://vibeage.eu/` and `https://mail.dmitrysamoylenko.in/` still return HTTP 200-level responses after cleanup.
- Verified external TCP checks for `2106`, `7777`, `8080`, `3001`, and `5432` are blocked.

## Remaining Cleanup

1. Keep watching the workstation backup timer for successful daily pulls.
2. Periodically run the Postgres restore drill.
3. Continue codebase cleanup toward shared protocol schemas, smaller simulation modules, and browser smoke coverage.
