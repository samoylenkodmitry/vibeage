version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "monthly"
      time: "04:00"
      timezone: "Europe/Belgrade"
    rebase-strategy: "disabled"
    groups:
      npm-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      - dependency-name: "@colyseus/core"
        update-types:
          - "version-update:semver-minor"
      - dependency-name: "@colyseus/ws-transport"
        update-types:
          - "version-update:semver-minor"
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
    # Keep version-update PRs disabled while the runtime architecture is moving.
    # Re-enable with a small nonzero limit when dependency work is intentional.
    open-pull-requests-limit: 0

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      time: "04:00"
      timezone: "Europe/Belgrade"
    rebase-strategy: "disabled"
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
    # Keep version-update PRs disabled while the runtime architecture is moving.
    # Re-enable with a small nonzero limit when dependency work is intentional.
    open-pull-requests-limit: 0

  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "monthly"
      time: "04:00"
      timezone: "Europe/Belgrade"
    rebase-strategy: "disabled"
    groups:
      docker-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
    # Keep version-update PRs disabled while the runtime architecture is moving.
    # Re-enable with a small nonzero limit when dependency work is intentional.
    open-pull-requests-limit: 0
